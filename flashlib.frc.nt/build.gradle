apply from: rootProject.file('jcomponent.gradle')

dependencies {
    api flashlib()
    api group: 'com.github.tomtzook', name: 'javabeans', version: '1.3.0'

    api group: 'edu.wpi.first.ntcore', name: 'ntcore-java', version: WPILIB_VERSION
}
