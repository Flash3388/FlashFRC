subprojects {
    group = GROUP
    version = VERSION

    repositories {
        mavenCentral()

        maven {
            url('https://frcmaven.wpi.edu/artifactory/release')
        }
    }
}

apply from: rootProject.file('flashfrc.gradle')
